{
  "name": "pr-rosey",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Electron app for monitoring the current GitHub user's pull requests.",
  "main": "out/main/index.js",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "typecheck": "tsgo --noEmit -p tsconfig.json && tsgo --noEmit -p tsconfig.electron.json",
    "format": "biome format --write .",
    "lint": "biome check .",
    "check": "npm run lint && npm run typecheck && npm test -- --run",
    "test": "vitest",
    "pr-watch": "node packages/pr-watch/src/cli.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.78.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@swc/core": "^1.15.40",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@typescript/native-preview": "^7.0.0-dev.20260527.2",
    "@vitejs/plugin-react": "^5.2.0",
    "autoprefixer": "^10.4.21",
    "electron": "^42.3.0",
    "electron-vite": "^5.0.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
